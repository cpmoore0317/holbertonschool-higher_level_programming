#!/usr/bin/node

class Rectangle {
  constructor (w, h) {
    // Constructor takes two arguments: w (width) and h (height).
    // Instance attributes width and height are initialized with the provided values.
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
